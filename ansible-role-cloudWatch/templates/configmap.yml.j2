# create configmap for cwagent config
kind: ConfigMap
metadata:
  name: {{ cloudWatch_configmap_name }}
  namespace: {{ cloudWatch_namespace }}
apiVersion: v1
data:
  cwagentconfig.json: |
    {
      "logs": {
        "metrics_collected": {
          "kubernetes": {
            "cluster_name": "{{ cloudWatch_k8s_cluster_name }}",
            "metrics_collection_interval": {{ cloudWatch_metrics_collection_interval }}
          }
        },
        "force_flush_interval": {{ cloudWatch_force_flush_interval }}
      }
    }

